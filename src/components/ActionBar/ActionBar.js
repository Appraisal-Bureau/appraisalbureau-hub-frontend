import './ActionBar.scss';

function ActionBar({ text, filterOptions, actionButtons, showViewButtons }) {
  return <div className="action-bar" data-testid="action-bar"></div>;
}

export default ActionBar;
